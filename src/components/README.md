# Shared Components

Use this folder only for components shared across multiple features.

```txt
components/
  ui/       # Button, Carousel, DropdownMenu, Pagination, Skeleton
  layout/   # Navbar, Footer, AppShell, PageContainer
```

If a component belongs to one feature, keep it in `src/features/<feature>/components`.
